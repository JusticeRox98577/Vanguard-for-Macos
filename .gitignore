# Build artifacts
build/
*.o
*.dSYM/
DerivedData/

# Compiled binaries (sign + build locally; never commit a signed binary)
Phase1-ProcessMonitor/build/
vanguard_monitor

# macOS cruft
.DS_Store

# Editor / IDE
.vscode/
.idea/
*.xcuserstate

# Node (Phase 2 server)
node_modules/
npm-debug.log*
.env

# Apple App Attest root CA — downloaded by the operator, never committed
# (pinning a hand-copied trust root risks silent transcription errors)
Apple_App_Attest_Root_CA.pem
